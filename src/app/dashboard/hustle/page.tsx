"use client";

import PillarPlaceholder from "@/components/PillarPlaceholder";

export default function HustlePage() {
    return (
        <PillarPlaceholder
            title="The Hustle"
            description="Job board, internship opportunities, and gig economy terminal."
        />
    );
}
