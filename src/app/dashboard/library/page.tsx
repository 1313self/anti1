"use client";

import PillarPlaceholder from "@/components/PillarPlaceholder";

export default function LibraryPage() {
    return (
        <PillarPlaceholder
            title="The Library"
            description="Access premium study materials, notes, and research papers."
        />
    );
}
